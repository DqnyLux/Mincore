name: Mincore
version: '1.0'
main: org.dqnylux.mincore.Mincore
loader: org.dqnylux.mincore.MincoreLoader

api-version: '1.21'
authors: [DqnyLux]

description: algoxd

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    LuckPerms:
      load: BEFORE
      required: false
    floodgate:
      load: BEFORE
      required: false